#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

export KOLABI_RUN_REPORT_COMMAND_NAME="$0"
exec python -m kolabi.bot.run_report "$@"
